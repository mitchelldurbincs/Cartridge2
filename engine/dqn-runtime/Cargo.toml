[package]
name = "dqn-runtime"
version = "0.1.0"
edition = "2021"
description = "Strict ONNX Q-value inference shared by DQN cartridge runtimes"

[dependencies]
algorithm-core = { path = "../algorithm-core" }
engine-core = { path = "../engine-core" }
anyhow = { workspace = true }
ort = "2.0.0-rc.10"
