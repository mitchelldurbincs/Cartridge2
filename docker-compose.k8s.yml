# Cartridge2 Docker Compose - K8s Backend Simulation
#
# This overlay adds PostgreSQL and MinIO for testing Kubernetes-style deployments locally.
# Use with the base docker-compose.yml:
#
#   docker compose -f docker-compose.yml -f docker-compose.k8s.yml up alphazero
#   docker compose -f docker-compose.yml -f docker-compose.k8s.yml up web frontend
#
# PostgreSQL replaces SQLite for the replay buffer.
# MinIO replaces filesystem for model storage.
#
# Web UI for MinIO: http://localhost:9001 (credentials from .env file)

# =============================================================================
# Extension Fields - Reusable configuration blocks
# =============================================================================

x-aws-env: &aws-env
  AWS_ACCESS_KEY_ID: ${MINIO_ROOT_USER:-minioadmin}
  AWS_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD:-changeme}
  AWS_REGION: us-east-1

x-storage-env: &storage-env
  <<: *aws-env
  CARTRIDGE_STORAGE_MODEL_BACKEND: s3
  CARTRIDGE_STORAGE_S3_BUCKET: cartridge-models
  CARTRIDGE_STORAGE_S3_ENDPOINT: http://minio:9000
  CARTRIDGE_STORAGE_REPLAY_BACKEND: postgres
  CARTRIDGE_STORAGE_POSTGRES_URL: postgresql://cartridge:cartridge@postgres:5432/cartridge

services:
  # ==========================================================================
  # Infrastructure Services
  # ==========================================================================

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: cartridge
      POSTGRES_PASSWORD: cartridge
      POSTGRES_DB: cartridge
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./scripts/init-postgres.sql:/docker-entrypoint-initdb.d/init.sql:ro
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cartridge -d cartridge"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - cartridge-net

  minio:
    image: minio/minio:RELEASE.2024-11-07T00-52-20Z
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-changeme}
    volumes:
      - minio_data:/data
    ports:
      - "9000:9000"
      - "9001:9001"
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - cartridge-net

  minio-setup:
    image: minio/mc:RELEASE.2024-11-05T11-29-45Z
    depends_on:
      minio:
        condition: service_healthy
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-changeme}
    entrypoint: >
      /bin/sh -c "
      mc alias set cartridge http://minio:9000 $${MINIO_ROOT_USER} $${MINIO_ROOT_PASSWORD};
      mc mb cartridge/cartridge-models --ignore-existing;
      mc anonymous set download cartridge/cartridge-models;
      echo 'MinIO bucket ready';
      "
    networks:
      - cartridge-net

  # ==========================================================================
  # Service Overrides for K8s Backends
  # ==========================================================================

  alphazero:
    build:
      args:
        CARGO_FEATURES: "s3"
    depends_on:
      postgres:
        condition: service_healthy
      minio-setup:
        condition: service_completed_successfully
    environment:
      <<: *storage-env

  web:
    build:
      args:
        CARGO_FEATURES: "s3"
    depends_on:
      minio-setup:
        condition: service_completed_successfully
    environment:
      <<: *aws-env
      CARTRIDGE_STORAGE_MODEL_BACKEND: s3
      CARTRIDGE_STORAGE_S3_BUCKET: cartridge-models
      CARTRIDGE_STORAGE_S3_ENDPOINT: http://minio:9000

volumes:
  postgres_data:
  minio_data:
